# DEPLOY — 2026-04-14

## What shipped

First production deploy of Greenloop Farm OS. Target: `local-docker` (primary). Public URL via Streamlit Cloud remains pending GitHub SSH auth.

- Commit: `ec057c3 build: Docker + CI + Streamlit Cloud deployment`
- Image: `greenloop-farm-os:latest` (180 MB, python:3.12-slim-bookworm base, libgomp1 for xgboost)
- Container: `greenloop` on port 8501, `--restart unless-stopped`
- Live URL: <http://localhost:8501> (HTTP 200 verified)

## Gates

- Tests: 116 passed (40.41 s)
- Docker build: clean, multi-stage, reproducible from HEAD

## Post-deploy

All four checks green (revision / traffic / user-visible / smoke). Full log at `deploy/deployments/2026-04-14-145111.md`.

## Cache invalidation

None — local Docker has no CDN in front. Streamlit Cloud deploy (when unblocked) handles its own.

## Notes

- `deploy/deployment-config.md` was onboarded in this same deploy. Two targets declared: `local-docker` (primary, working now) and `streamlit-cloud` (secondary, pending push).
- `uv run pytest -q` hit a 116-test pass after one `chflags -R nohidden .venv` — the macOS UF_HIDDEN trap re-appeared, as expected. Fix is documented in project memory.
- Container is persistent across laptop reboots (`--restart unless-stopped`). Stop with `docker stop greenloop`.

## Phase 1 Pilot Plan

Three farms, 12-week pilot (Q3 2026) to validate the Greenloop Farm OS before full commercial rollout.

| Dimension | Detail |
|-----------|--------|
| Farms | 3 farms (Jurong, Lim Chu Kang, Woodlands) |
| Duration | 12 weeks per pilot farm |
| Investment | SGD 80K total (setup + monitoring) |
| Goal | Validate demand forecast accuracy, UX, operational ROI |
| Success criteria | >70% gross margin, <3mo CAC payback |
| Pilot period | Jul–Sep 2026 |
| Post-pilot | Scale to 10 farms on validation |
