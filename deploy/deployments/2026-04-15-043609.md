# Deploy Log — 2026-04-15 04:36:09 UTC

## Summary

| Field | Value |
|---|---|
| Target | `local-docker` |
| Commit | `a1a46c3 chore(deploy): record 10-crop deploy (2026-04-15)` |
| Previous | `088660f` (10-crop feature) |
| Image | `adopt-a-kale:latest` (cache-hit; no layer rebuild needed) |
| Container | `adoptakale` (`7afdf9e06c80`) |
| Live URL | <http://localhost:8501> |
| Outcome | ✅ Deployed + verified |

## Nature of this deploy

**Refresh deploy with zero production drift.** `git log 088660f..HEAD -- src/ data/ models/ Dockerfile pyproject.toml uv.lock requirements.txt streamlit_app.py` returned no commits — only `deploy/deployments/` and `deploy/.last-deployed` changed between the previous deploy and this one.

Ran /deploy anyway because:
1. The invocation was explicit.
2. The state file was pinned at `088660f` while HEAD was at `a1a46c3`; cleaning up keeps `/deploy --check` honest.
3. Verifies the container is still serving what it claims to serve.

## Gates

| Gate | Result |
|---|---|
| `pytest -q` | 132 passed in 40.97 s |
| `docker build` | Cache hit (no layer rebuild) — no prod-path changes |

## Post-deploy checks

| Check | Result |
|---|---|
| Revision | `image=adopt-a-kale:latest status=running started=2026-04-15T04:29:22Z` |
| Traffic | `8501/tcp -> 0.0.0.0:8501` + IPv6 (local Docker, no split) |
| User-visible | `status=200 time=2.55ms size=4876B` (no-cache headers sent) |
| Smoke | Streamlit marker present in HTML; `/_stcore/health` → `200` |
| Cache invalidation | N/A (local Docker, no CDN) |

## Rollback

```bash
docker stop adoptakale
git reset --hard 088660f    # if rolling back production
docker build -t adopt-a-kale:latest . && \
  docker run -d --name adoptakale --restart unless-stopped \
    --env-file .env -p 8501:8501 adopt-a-kale:latest
```
