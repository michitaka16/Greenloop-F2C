# Deploy Log — 2026-04-14 14:51:11 UTC

## Summary

| Field | Value |
|---|---|
| Target | `local-docker` (primary) |
| Commit | `ec057c319aa4c3b3eddff159affd1ecc02f40c36` |
| Image | `greenloop-farm-os:latest` (sha256 `2ffeb55d37f27dc0ea6096f4c1bb1918984083ee6b1a07afa8e3325504effa9b`) |
| Container | `greenloop` (`ebe5f91adf09`) |
| Live URL | <http://localhost:8501> |
| Outcome | ✅ Deployed + verified |

## What was deployed

First formal deploy for this repo. Two HEAD commits:

```
ec057c3 build: Docker + CI + Streamlit Cloud deployment
afe022e feat: full 3-layer farm OS with Streamlit dashboard
```

## Gates

| Gate | Result |
|---|---|
| `uv run pytest -q` | 116 passed in 40.41 s |
| `docker build -t greenloop-farm-os:latest .` | Built — image sha `2ffeb55d37f2…`, 12-step multi-stage |

## Post-deploy checks

| Check | Command | Result |
|---|---|---|
| Revision | `docker inspect --format='{{.Config.Image}} {{.State.Status}}' greenloop` | `greenloop-farm-os:latest running` |
| Traffic | `docker port greenloop` | `8501/tcp -> 0.0.0.0:8501` + IPv6 |
| User-visible | `curl -fsS http://localhost:8501/` | HTTP `200` |
| Smoke | `curl http://localhost:8501/ \| grep Streamlit` | `ok` |

## Streamlit Cloud (secondary target)

Blocked on GitHub SSH auth. Public deploy will proceed once `git push origin main` succeeds — see `DEPLOY.md` for the 3-field Streamlit Cloud form.

## Rollback

```bash
# Stop the current container
docker stop greenloop

# Previous state: no container running (this is the first deploy).
# Remove to clean up:
docker rm -f greenloop
```
