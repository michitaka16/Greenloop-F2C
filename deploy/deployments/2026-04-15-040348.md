# Deploy Log — 2026-04-15 04:03:48 UTC

## Summary

| Field | Value |
|---|---|
| Target | `local-docker` |
| Commit | `088660f feat(crops): expand from 5 to 10 Singapore-viable crops` |
| Previous | `37b8981` (LLM feature) |
| Image | `adopt-a-kale:latest` |
| Container | `adoptakale` (`e76c5ba40dd`) |
| Live URL | <http://localhost:8501> |
| Outcome | ✅ Deployed + verified |

## What shipped

5 new crops: `pak_choi`, `kale`, `basil_thai`, `coriander`, `mint`. All are commercially grown by SFA-licensed Singapore vertical farms. Models retrained; dashboard, CLI, and optimizer auto-adapt through `CROP_IDS`.

## Gates

| Gate | Result |
|---|---|
| `pytest -q` | 132 passed in 24.02 s |
| `docker build` | ~50 s |
| Layer 1 retrain CV MAE | q05: 9.98 / q50: 6.84 / q95: 7.01 kg (on 1400 feature rows × 10 crops) |

## Post-deploy

| Check | Result |
|---|---|
| Revision | `adopt-a-kale:latest running` |
| User-visible | HTTP `200` |
| CLI smoke | `adoptakale solve` → profit $153.98, 41.9 ms; `adoptakale forecast` → 10 rows |

## Rollback

```bash
git revert 088660f          # 5-crop state
docker build -t adopt-a-kale:latest . && \
  docker rm -f adoptakale && \
  docker run -d --name adoptakale --restart unless-stopped \
    --env-file .env -p 8501:8501 adopt-a-kale:latest
```
