# Deploy Log — 2026-04-15 03:05:03 UTC

## Summary

| Field | Value |
|---|---|
| Target | `local-docker` (primary) |
| Commit | `37b8981` (on `origin/main`) |
| Previous deploy | `ec057c3` (local-docker, 2026-04-14 14:51) |
| Image | `adopt-a-kale:latest` (rebuilt, new layer: `anthropic==0.95.0` + `openai==2.31.0`) |
| Container | `adoptakale` (`dd93295949196`), restart `unless-stopped`, `--env-file .env` |
| Live URL | <http://localhost:8501> |
| Outcome | ✅ Deployed + verified |

## What shipped in this deploy

```
37b8981 feat(llm): provider-agnostic plan explainer — OpenAI / Z.AI / MiniMax
5c9c07e chore(deploy): onboard deployment-config + first deploy log
```

- AI narrative expander in the dashboard (`_render_ai_explainer`)
- New package `adoptakale.llm` (client gate + plan_explainer prompt)
- New utility `adoptakale.utils.llm.chat` (provider-agnostic transport)
- 16 new unit tests — total suite 132 passed
- `anthropic>=0.40` added as prod dep
- `requirements.txt` regenerated (110 packages)
- `deploy/deployment-config.md` updated to include `--env-file .env` in the deploy command so LLM creds reach the runtime

## Gates

| Gate | Result |
|---|---|
| `pytest -q` | 132 passed in 36.34 s |
| `docker build -t adopt-a-kale:latest .` | Built, 50 s wall clock |

## Post-deploy checks

| Check | Result |
|---|---|
| Revision | `adopt-a-kale:latest running` |
| Traffic | `8501/tcp -> 0.0.0.0:8501` + IPv6 |
| User-visible | HTTP `200` |

## Rollback

```bash
docker stop adoptakale
# Previous image is still in the local registry:
docker run -d --name adoptakale --restart unless-stopped \
  --env-file .env -p 8501:8501 adopt-a-kale:latest
# To pin to a specific prior image, tag at build time and reference that tag.
```
